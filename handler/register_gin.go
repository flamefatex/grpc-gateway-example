package handler

import (
	"context"

	"github.com/gin-gonic/gin"
)

// ExecRegisterGinHandler 注册gin服务
func ExecRegisterGinHandler(ctx context.Context, ginRouter *gin.Engine) {
	// 注册
	//gGroup := ginRouter.Group("/g")
	//opsV1Group := gGroup.Group("/api/v1")
}
